# Source map

源码来自：

```text
gwj_reinstall_backup_20260724/restored/Simulation
```

只复制了 `configs/`、`scripts/`、`src/`、`tests/`、`pyproject.toml` 和项目文档。原 `config/` 已统一重命名为 `configs/`。

明确排除：

- `smplx_models/`
- `test_motion/`
- `outputs/`
- BlenderProc 官方源码
- Isaac Sim 安装目录和 Docker 镜像
- Python `*.egg-info`
